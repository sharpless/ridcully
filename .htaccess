<IfModule mod_rewrite.c>
  RewriteEngine on
  # Change RewriteBase if needed, or delete it depending on your setup
  RewriteBase /phpmvc/kmom03/ridcully/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule (.*) index.php/$1 [NC,L]
</IfModule>